# Local dev/test only. TCP connections still require SCRAM passwords, but
# addresses are intentionally broad because this Docker Compose stack is isolated.
local all all trust
host all all all scram-sha-256
host replication all all scram-sha-256
