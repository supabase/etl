[package]
name = "etl-destinations"
version = "0.1.0"
edition = "2024"

[features]
bigquery = ["dep:etl-destinations-bigquery"]
iceberg = ["dep:etl-destinations-iceberg"]

[dependencies]
etl-destinations-bigquery = { path = "bigquery", optional = true }
etl-destinations-iceberg = { path = "iceberg", optional = true }
