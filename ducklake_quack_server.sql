-- ============================================================================
-- STEP 6: Start Quack server
-- ============================================================================
CALL quack_serve('quack:localhost', token = 'super_secret');
